import React from 'react';

export const Login = (props) => {
  {/*
    - check for error on redirect parameters
    - pull code redirect parameter from url
    - make POST to /oauth2/token endpoint
    - store access token, refresh token, and user on session
      - subsequently fetch user from server by userId
  */}

  return (
    <h1>/login</h1>
  );
};
